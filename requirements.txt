# Versions pinned to what was last verified on this project (Python 3.13).
# Bump intentionally — not silently via `pip install -U`.

# ── LLM ──────────────────────────────────────────────────────────────────────
openai==2.26.0              # OpenAI API (gpt-4.1 answers, gpt-4o-mini query expansion)
tiktoken==0.12.0            # Token counting for context window management

# ── Embedding ─────────────────────────────────────────────────────────────────
voyageai==0.3.7             # Voyage AI API client (voyage-code-3)
# sentence-transformers     # Uncomment when switching to nomic_local
# torch                     # Uncomment when switching to nomic_local

# ── Vector Database ───────────────────────────────────────────────────────────
pymilvus==2.6.9             # Milvus Python client

# ── Code Chunking ─────────────────────────────────────────────────────────────
tree-sitter==0.23.2         # Pinned — API changed in 0.22+
tree-sitter-python==0.23.6  # Python grammar for tree-sitter
# tree-sitter-java          # Uncomment when adding Java support
# tree-sitter-scala         # Uncomment when adding Scala support

# ── Git Integration ───────────────────────────────────────────────────────────
gitpython==3.1.46           # git diff, pull, commit tracking

# ── Web Server ────────────────────────────────────────────────────────────────
fastapi==0.135.1            # REST API for web UI
uvicorn==0.41.0             # ASGI server for FastAPI
pydantic==2.12.5            # Request/response models (FastAPI dependency)

# ── CLI ───────────────────────────────────────────────────────────────────────
click==8.3.1                # CLI framework
rich==14.3.3                # Terminal output + progress bars

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.2.2        # .env file loading
numpy==2.4.3                # Transitive dependency (required by voyageai and pymilvus)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==9.0.2               # Test runner
